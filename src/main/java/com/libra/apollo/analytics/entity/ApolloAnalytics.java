package com.libra.apollo.analytics.entity;

import java.util.SortedSet;
import java.util.TreeSet;

import javax.persistence.CascadeType;
import javax.persistence.Column;
import javax.persistence.Embedded;
import javax.persistence.Entity;
import javax.persistence.EnumType;
import javax.persistence.Enumerated;
import javax.persistence.FetchType;
import javax.persistence.Index;
import javax.persistence.OneToMany;
import javax.persistence.Table;
import javax.validation.constraints.NotNull;

import org.apache.commons.lang.builder.CompareToBuilder;
import org.hibernate.annotations.SortNatural;

import com.libra.apollo.analytics.entity.enums.AnalyticsType;
import com.libra.apollo.analytics.entity.enums.RunType;

@Entity
@Table(name = "apollo_analytics", schema="analytics", indexes = { @Index(name = "type_idx", columnList = "analytics_type"),
		@Index(name = "run_type_idx", columnList = "run_type"), }

)
@SuppressWarnings("serial")
public class ApolloAnalytics extends AutoGeneratedId implements Comparable<ApolloAnalytics> {

	@Embedded
	private Definition analyticsDefinition;

	@Enumerated(EnumType.STRING)
	@Column(name = "analytics_type", nullable = false)
	private AnalyticsType type;
	
	@Column(name="run_type")
	@NotNull
	@Enumerated(EnumType.STRING)
	private RunType runType;

	@OneToMany(mappedBy = "analytics", cascade = CascadeType.ALL, fetch = FetchType.LAZY)
	@SortNatural
	private SortedSet<AnalyticsView> views = new TreeSet<>();

	@OneToMany(mappedBy = "analytics", cascade = CascadeType.ALL, fetch = FetchType.LAZY)
	@SortNatural
	private SortedSet<InvestmentStyle> investmentStyles = new TreeSet<>();

	public Definition getAnalyticsDefinition() {
		return analyticsDefinition;
	}

	public void setAnalyticsDefinition(Definition analyticsDefinition) {
		this.analyticsDefinition = analyticsDefinition;
	}

	public AnalyticsType getType() {
		return type;
	}

	public void setType(AnalyticsType type) {
		this.type = type;
	}

	public RunType getRunType() {
		return runType;
	}

	public void setRunType(RunType runType) {
		this.runType = runType;
	}


	public SortedSet<AnalyticsView> getViews() {
		return views;
	}

	public void setViews(SortedSet<AnalyticsView> views) {
		this.views = views;
	}


	public SortedSet<InvestmentStyle> getInvestmentStyles() {
		return investmentStyles;
	}

	public void setInvestmentStyles(SortedSet<InvestmentStyle> investmentStyles) {
		this.investmentStyles = investmentStyles;
	}
	

	@Override
	public int hashCode() {
		final int prime = 31;
		int result = super.hashCode();
		result = prime * result + ((analyticsDefinition == null) ? 0 : analyticsDefinition.hashCode());
		result = prime * result + ((runType == null) ? 0 : runType.hashCode());
		result = prime * result + ((type == null) ? 0 : type.hashCode());
		return result;
	}

	@Override
	public boolean equals(Object obj) {
		if (this == obj)
			return true;
		if (!super.equals(obj))
			return false;
		if (getClass() != obj.getClass())
			return false;
		ApolloAnalytics other = (ApolloAnalytics) obj;
		if (analyticsDefinition == null) {
			if (other.analyticsDefinition != null)
				return false;
		} else if (!analyticsDefinition.equals(other.analyticsDefinition))
			return false;
		if (runType != other.runType)
			return false;
		if (type != other.type)
			return false;
		return true;
	}

	@Override
	public int compareTo(ApolloAnalytics o) {
		return new CompareToBuilder().append(this.getType(), o.getType())
				.append(this.getAnalyticsDefinition().getName(), o.getAnalyticsDefinition().getName()).toComparison();
	}

}
